섹션 2 강의노트: https://drive.google.com/file/d/1udx72tv84NVeZNYzonGrCj6udhGTlINj/view?usp=drive_link

1. concatenation: 합치는 작업을 말함

2. 함수가 반드시 리턴을 해야 영향력을 끼치는게 아니다.
| **구분** | **데이터 타입** | **특성 (함수 내부 동작)** | **리턴 필요 여부** |
| --- | --- | --- | --- |
| **불변 (Immutable)** | `int`, `float`, `str`, `tuple`, `bool` | 함수 안에서 값을 바꾸려 하면 **새로운 객체**를 만듦. 원본은 절대 안 변함. | **필수** (리턴해서 덮어씌워야 함) |
| **가변 (Mutable)** | `list`, `dict`, `set`, 사용자 정의 클래스 | 함수 안에서 내부를 수정하면 **원본 메모리 주소**의 데이터가 즉시 변함. | **선택** (리턴 안 해도 원본이 변함) |

3. try-except를 안전하게 쓰기 위한 5대 에러 표
| **에러 이름** | **발생 상황 (예시)** |
| --- | --- |
| **`ValueError`** | 타입은 맞는데 값이 이상할 때 (`int("abc")`) |
| **`TypeError`** | 데이터 타입 자체가 틀렸을 때 (`len(123)`) |
| **`IndexError`** | 리스트의 없는 번호를 불렀을 때 (`[1, 2][5]`) |
| **`KeyError`** | 딕셔너리에 없는 키를 찾을 때 (`{"a":1}["b"]`) |
| **`AttributeError`** | 없는 기능을 실행할 때 (`"abc".not_exist_func()`) |

except: 는 모든 사고기록을 말소하는 블랙홀이다. 사용자의 ctrl c 에러까지도 잡아먹어 죽지도 못하게 만든다.

`except Exception:`은 왜 안전한가?

반면 현업에서 권장하는 **`except Exception:`**이나 더 구체적인 **`except ValueError:`**는 안전합니다.

**`KeyboardInterrupt`는 `Exception`의 자식이 아닙니다.** 서로 옆집 사는 사이(sibling)입니다.

- 따라서 `except Exception:`이라고 적으면, "일반적인 사고(`Exception`)만 잡고, **시스템 신호(`KeyboardInterrupt`, `SystemExit`)는 건드리지 마라**"는 뜻이 됩니다.
- 이렇게 짜야만 `Ctrl+C`를 눌렀을 때 프로그램이 정상적으로 종료됩니다.

4. **프로그래밍에서 '변수(Variable)'는 주로 어떤 역할을 하나요?**

A. 데이터를 저장하는 메모리 공간의 이름표 역할

해설: 변수는 프로그램 실행 중에 사용되는 데이터를 저장하기 위해 할당된 메모리 공간에 붙여진 이름입니다. 데이터를 담아두고 필요할 때 사용하죠.

5. **Python에서 `//` 연산자는 어떤 역할을 하나요?**

A. 나눗셈의 몫 중 소수점 이하를 버리고 정수 부분만 구한다

해설: `//`는 '몫 연산자' 또는 '정수 나눗셈'이라고 불립니다. 나눗셈 결과에서 소수점 이하는 버리고 정수 몫만 반환하는 연산자입니다.